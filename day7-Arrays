import math
import os
import random
import re
import sys



if __name__ == '__main__':
    n = int(input().strip())

    arr = list(map(int, input().rstrip().split()))
    reversed_array = arr[::-1]
    reversed_list = ''
    
    print(*reversed_array,sep=' ')
